fastapi
uvicorn
# WebSocket 経路 (v2 export 本実装) で必須。
# `uvicorn[standard]` を使えば一緒に入るが、Windows + 一部 pip 環境で extras が
# 解決されず websockets 抜けが起きるため、ここで明示する。
#
# 重要 (書き出し速度): websockets は受信フレームのアンマスク等を C 拡張
# `websockets.speedups` で高速化する。これが wheel に同梱されていないと
# pure-Python にフォールバックし、Windows で動画書き出しが本来の 1/4〜1/6 まで
# 遅くなる (2026-06-02 調査)。speedups 入りの wheel は Python 3.12 / 3.13 で広く
# 提供されているため、書き出し速度が問題になる環境では Python 3.12 / 3.13 を推奨。
# 現在の構成が degraded かどうかは全体設定→アップデートタブの「実行環境診断」で
# 確認できる (websockets.speedups の有無を表示)。
websockets>=12
python-multipart
pillow
psd-tools
# numpy は visualizer プラグインの FFT / 解析、compositor の Pillow 内部処理
# (remove_exact_white 等) で直接使う。psd-tools 経由の暗黙依存にせず明示する。
numpy
# OS/2.usWeightClass + name テーブルからフォントの正規 weight / family を取り出す
# (filename ベースの推定だけだと "AwesomeFont-XYZ.otf" のような独自命名が
# Regular にフォールバックしてしまうため)。
fonttools
# JS vendor 取得 (Noto Sans JP / three / mp4box) で使用。
httpx
# macOS: CoreText 経由で「登録済みフォントの実ファイルパス」を列挙する
# (app/system_fonts.py)。Adobe Fonts 等の仮想インストールフォントを標準
# ディレクトリ走査に頼らず正攻法で検出できる。未導入でも CoreSync
# ディレクトリの直接走査でフォールバックするため必須ではない。
pyobjc-framework-CoreText ; sys_platform == "darwin"
# 配布用 import_manifest.yml の読み書きに使用 (Phase 5 で独自パーサから移行)。
PyYAML>=6.0
