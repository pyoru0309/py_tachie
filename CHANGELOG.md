# Changelog

このファイルは py_tachie の変更履歴を記録します。
バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [Unreleased]

## [0.4.0] - 2026-09-07

演出まわりの追加が中心のアップデートです。カット全体をゆっくり動かす
「ケンバーンズ」、前景・背景の拡大率と表示位置、キャラの立ち位置を覚えておける
「配置プリセット」を追加しました。

（本リリースは py_tachie/dev チャネルで先行配信していた変更を、検証を経て
stable に取り込んだものです。）

### Added
- **ケンバーンズ（シーン全体のズーム・パン）**。演出タブに追加されました。カットの
  先頭から末尾までをかけて、画面全体をゆっくり拡大・移動させる演出です。静止画に
  動きを与えて、会話シーンに緩やかな流れを作れます。
  - 開始 / 終了 の「拡大率」「X」「Y」と「変化のしかた」（ゆっくり始まりゆっくり
    止まる / 一定速度 / ゆっくり始まる / ゆっくり止まる）を指定します。拡大率は
    画面中央を基準にした倍率で、1.0 が等倍です。
  - **動くもの**: 背景画像・背景動画・前景・キャラクター・動画レイヤー・
    ビジュアライザー・キャラ配置の分割線。
    **動かないもの**: セリフ枠と本文・テロップ・背景色の塗りつぶし・トランジション。
    文字が絵と一緒に動くと読みづらいため、セリフとテロップは常に画面に固定されます。
  - 「開始と終了を入替」でズームイン ⇔ ズームアウトを反転、「リセット」で等倍に
    戻せます。「他カットへ一括適用」で同じ設定を他のカットへ配れます。
  - キャラごとの「拡大」「移動」モーションとは独立して重ね掛けできます
    （ケンバーンズ＝カメラを引く / 寄せる、キャラモーション＝被写体が動く）。
  - プレビューと書き出しの両方に同じだけ効きます。ケンバーンズが効いているカット
    でも、プレビュー上のキャラのドラッグ配置はそのまま使えます。
- **前景・背景の拡大率と、背景の表示位置**。演出タブの「背景・場面」に
  「前景 拡大率」「背景 X / Y」「背景 拡大率」を追加しました。
  - 拡大率は前景が「画面に収まるサイズ」、背景が「画面いっぱい（1920×1080）」を
    1.0 とした倍率です（0.05〜4.0）。
  - **ケンバーンズでズームアウトさせるときは、あらかじめ背景 / 前景の拡大率を
    1.1〜1.3 程度に上げて余白を作っておく**と、画面端が透けません。1920×1080 より
    大きめの画像を用意して拡大率で調整する使い方もできます。
  - 前景・背景それぞれに「左右中央」「上下中央」ボタンを追加しました。押すと座標欄が
    空欄（＝中央配置）に戻ります。空欄は「拡大率を反映したサイズで中央寄せ」なので、
    後から拡大率を変えても中央のまま保たれます。
- **配置プリセット（キャラの立ち位置の保存）**。キャラ配置タブに追加されました。
  編集中のキャラの **X / Y / 拡大率** の 3 つだけを名前付きで保存し、別のカットで
  呼び出せます。「左寄り」「アップ」「引き」のような立ち位置テンプレートを作って
  おけば、カットごとに構図を揃えるのが一発になります。
  - **表情プリセットとは完全に別系統**です。配置プリセットを適用しても表情・髪型・
    前面オブジェクトは変わりませんし、表情プリセットを切り替えても立ち位置は
    変わりません。
  - プリセットはキャラクターごとに分かれています。セレクタには
    `名前（X 100 / Y 12 / ×0.9）` のように中身も表示されます。
  - 既存のプリセットを選んだ状態で保存すると上書き、「なし」の状態で保存すると
    新規作成です。現在の X / Y / 拡大率 と一致するプリセットがあれば、カットを
    移動したときに自動で選択状態になります。
  - 保存先はプロジェクト単位（`projects/<project_id>/placement_presets.json`）です。

## [0.3.0] - 2026-08-03

カットのトランジション（場面転換効果）の刷新、PC インストール済みフォントの利用、
テロップの日本語縦書きなど、編集まわりを大きく強化したアップデートです。

（本リリースは py_tachie/dev チャネルで先行配信していた一連の変更を、Mac/Windows
両方での検証を経て stable に取り込んだものです。）

### Added
- **PC インストール済みフォントの利用**。アセットにフォントファイルを置かなくても、
  PC にインストールされているフォントが書体セレクタに載るようになりました。
  Adobe Fonts のような「アクティベート型」のフォントにも対応します
  （macOS は CoreText の登録情報、Windows はレジストリと Adobe のフォルダも参照）。
  - 一覧はアプリ起動時にバックグラウンドでスキャンされ、2 回目以降はキャッシュから
    即座に読み込まれます。フォントを追加・アクティベートした直後は
    全体設定 → 環境タブ の「PC フォントを再スキャン」を押してください。
  - 「PC のインストール済みフォントを使う」をオフにすると、従来どおりアセット内
    フォントだけになります。プロジェクトを別の PC で開く場合、同じフォントが
    インストールされていないと代替書体で表示されます（配布・共同編集する
    プロジェクトでは従来どおりアセット内フォントの利用が確実です）。
  - macOS では `python3 -m pip install -r requirements.txt` の再実行を推奨します
    （Adobe Fonts 検出用の CoreText 連携が追加されました。未導入でもフォルダ走査で
    動作します）。
- **テロップの日本語縦書き**。テロップ編集の「書字方向」で縦書きを選べます。
  改行が行（カラム）の区切りになり、行は右から左へ並びます。
  - 句読点（、。）・括弧類（「」（）など）・長音（ー）・三点リーダ（…）・
    小書き仮名（っゃ など）は、フォント自身が持つ縦書き用の字形
    （OpenType GSUB `vert` フィーチャ）で描画されます。縦書き字形を持たない
    フォント（ピクセルフォントなど）は文字種に応じた自動変換（90° 回転 /
    右上シフト）で近似します。
  - 縦書きでは「位置」と「文字揃え」の軸が入れ替わります。「位置」は行を
    置き始める側（右 / 中央 / 左）、「文字揃え」は行内の揃え（上 / 中央 / 下）です。
  - 光彩・ドロップシャドウ・縁取り・登場退場アニメ・文字単位アニメ
    （タイプライター等）は縦書きでもそのまま使えます。オプティカルカーニングは
    横書き専用のため縦書きでは無効になります。セリフは従来どおり横書きのみです。
- **カット入りトランジション（境界またぎ）**。直前のカットと現在のカットの境目で、
  効果時間を前後半分ずつシェアして切り替えます。トランジション中も両方のカットが
  動き続けます（口パク・目パチ・モーション・BPM ゆれが止まりません）。
  - 種類: クロスフェード / ワイプ（左右上下の方向指定）/ ホワイトアウト・イン /
    ブラックアウト・イン / クロスズーム。
  - 先頭カットでは「白から」「黒から」のフェードインになります。
- **発話ディレイ**。カットの話者音声を冒頭から指定秒だけ遅らせて鳴らせます
  （トランジションの最中に声が出始める不自然さを避けられます）。
- **項目のコピー＆ペースト**。カット / テロップ / 効果音 / 動画レイヤーを
  `Cmd/Ctrl+C`・`Cmd/Ctrl+V`（または「操作」メニュー）でコピー＆貼り付けできます。
  カットをコピーすると、リンクしたテロップ・効果音・動画も一緒に複製されます。
- **タイムラインのレーン（段）追加**。テロップ / 効果音 / 動画レイヤーを複数の段に
  分けて並べられます。
- **字幕データ（SRT / VTT）書き出し**。テロップとセリフを、それぞれ SRT と VTT の
  字幕ファイルとして `outputs/` へ同時に書き出せます（どちらも UTF-8）。
  - 表示時刻はカット / テロップの配置からそのまま算出されます。
  - セリフには話者名（キャラ表示名）が付き、VTT は `<v 話者名>`、SRT は行頭の
    「話者名：」として入ります。テロップには話者名は付きません。
  - VTT には文字スタイルや表示位置の情報は含めず、本文だけを書き出します
    （字幕編集ソフトや動画サイトへの取り込み向け）。

### Changed
- **書き出しボタンを「エクスポート ▼」に統合**。ヘッダーの「PNG出力」「動画出力」を
  一つのプルダウンにまとめ、`PNG静止画` / `動画` / `テロップデータ (SRT/VTT)` /
  `セリフデータ (SRT/VTT)` から選んで書き出せるようにしました。
- **個別字間の微調整**。セリフ・テロップともに `−10` / `＋10` ボタンを追加し、
  10 単位で細かく詰められるようになりました（従来は ±100 のみ）。全体の文字間も
  10 単位で入力できます。
- **セリフ枠のレイアウト**。折り返し枠を先に確定してから文字を配置するようにし、
  詰めたり文字間をマイナスにしても右側だけ余って見える現象を解消しました。
- **「テロップに一括反映」の対象に「書字方向」「文字揃え」を追加**。既存の
  テロップをまとめて縦書きへ切り替える、といった変更が一度にできます。
- アプリのバージョン表記を Semantic Versioning（例 `v0.3.0-dev`）に統一しました。

### Fixed
- サーバをリポジトリ以外のフォルダから起動したとき、セリフの折り返し計算だけが
  別のフォントで行われてプレビューとずれることがある問題を修正しました。
- テロップの個別字間を編集しても、静的キャッシュが残って画面に反映されない
  ことがある問題を修正しました。

## [0.2.2] - 2026-06-27

テロップ・セリフのオプティカルカーニング（高品質）の字間を見直し、文字間が揃って
締まって見えるよう改善したアップデートです。

### Changed
- **オプティカルカーニング（高品質）の字間を改善**。文字ごとの間隔が不揃いになる問題を
  解消し、和文・欧文それぞれの中で字間が揃うようにしました。あわせて全体に程よく詰めて、
  まとまりのある締まった組みになります。
  - 揃える基準を「行の平均アキ」から「実際に見える字面どうしの隙間」に変更
    （平均アキは字形によって見た目の隙間と食い違い、揃えるほど不揃いに見える原因でした）。
  - 和文（漢字・かな）と欧文（英数字）で別々の基準を持たせ、和欧が混ざる行でも
    それぞれ自然な字間になるようにしました。
  - 句読点・括弧などの約物は前後を詰めて、大きな隙間（穴）ができないようにしました。
  - 字面が触れて窮屈にならないよう最低限のアキを確保（縁取りありでも線が融合しません）。
  - ゆったりした書体ほど強めに詰めて、書体が変わっても字間の印象が揃うようにしました。

## [0.2.1] - 2026-06-21

書き出しの高速化・安定化と、編集まわりの機能追加・不具合修正をまとめたアップデート。
特に背景動画を含む書き出しの停止・フリーズや、ビジュアライザー/フォントの不具合を解消し、
Mac での書き出し速度を大きく改善しました。

### Added
- **事前解析 (プリレンダー)**: タイムラインのツールバーに「事前解析」ボタンを追加。
  全カットの解析キャッシュ (ビジュアライザー解析・焼き込み) を書き出し前にまとめて生成し、
  書き出しを高速化できます。タイムライン最上部の帯が 緑=解析済 / 赤=解析中 / 灰=未解析 で
  状態を表示し、再生中の先読みでも自動的に緑になります。
- **テロップのオプティカルカーニング (per-telop)**: テロップごとに「カーニング」
  (継承＝全体設定 / OFF / 標準 / 高品質) を設定できるようにしました。MV 文字への変換や
  アニメーション付与でもカーニング設定が反映されます。一括反映の対象項目にも追加しました。

### Changed
- **書き出しの高速化**: 口パク解析 (lipsync) を音源全長で 1 回だけ解析してカット範囲を
  切り出す方式にし、同じ BGM を共有する多カット書き出しで ffmpeg の起動回数を大幅削減
  (実測でカット単位解析の数十倍速)。ビジュアライザー解析キャッシュの実効化、キャラレイヤー
  PNG の高速エンコード等とあわせ、Mac での書き出し速度を大きく改善しました。
- **書き出しログの改善**: ログを全選択コピーしやすいテキストエリア表示に変更し、
  プラットフォーム情報 (OS / ブラウザ / GPU) と、律速の切り分けに使える内訳計測
  (render / エンコード待ち / カット境界 / サーバ内訳) をサマリに追加しました。
- 一括適用ダイアログの前景項目を「前景 / 表示位置」表記に統一し、表示位置 (X/Y) も
  併せて反映されることを明示しました。

### Fixed
- **背景動画の書き出しが途中で止まる/フリーズする不具合を修正**。WebCodecs デコードの
  出力サーフェス枯渇 (背景動画が途中フレームで停止) と、末尾フレームの取りこぼし、
  および一部環境でのフレーム途中フリーズ (キャンセル不能) を解消しました。
- **ビジュアライザーの解析回帰を修正**し、解析キャッシュを正しく効かせるようにしました。
  現在のカット表示前に音源全長の解析が走って初動が重くなる問題も解消しています。
- **プロジェクトを複製するとアセットの参照先が複製元のままになる不具合を修正**。複製後は
  複製先のアセットを正しく参照します (複製元を削除しても壊れません)。
- **プロジェクト切り替え時にフォントが反映されない不具合を修正** (リロード不要になりました)。
- Windows: 書き出し/プレビュー中に出ていた `ConnectionResetError (10054)` のログノイズを
  抑止し、動画レイヤープレビューの読み込み遅延を軽減しました。
- 口パク中心 (カットごとに異なる音声) のプロジェクトで、書き出しがかえって遅くなる
  回帰を修正しました。

## [0.2.0] - 2026-06-02

Windows での動画書き出しの実用化を中心とした大型アップデート。書き出し経路を
WebGL → WebSocket → ffmpeg のまま、**ブラウザ内 WebCodecs で H.264 に圧縮してから
送る**方式 (transport=webcodecs-h264) を追加し、Windows の書き出しを大幅に高速化・
安定化しました。あわせて、起動できない/書き出しが固まる Windows 由来の不具合と、
アップデート機能の堅牢化を行いました。

### Added
- **動画書き出しの高速化 (WebCodecs)**: 書き出しダイアログに「高速書き出し
  (WebCodecs)」トグル (既定 ON) を追加。H.264 mp4 プリセットのとき、ブラウザ内で
  H.264 に圧縮してから送り、サーバは ffmpeg `-c copy` でコンテナ化のみ行います。
  ブラウザ→サーバの転送量が 1/数百〜数千 に激減し、特に Windows で書き出しが大幅に
  速くなります。透過 (ProRes/PNG) や非対応ブラウザでは自動的に従来方式へフォール
  バックします。
- **実行環境診断**: 全体設定 → アップデートタブに、Python / websockets / 高速化拡張
  (websockets.speedups) の状態を表示する診断パネルを追加。書き出しが遅くなる構成を
  早期に発見できます。
- **復旧スクリプト**: `tools/recover.bat` (Windows) / `tools/recover.sh` (macOS/Linux)
  を同梱。アップデート失敗で起動しなくなったとき、ダブルクリック等でリモートの最新
  状態へ復旧できます (プロジェクト等のデータは保持)。

### Changed
- **アップデートの適用方式を `git pull` (merge) から `fetch + reset --hard` に変更**。
  作業ツリーが壊れていても確実にリモートと一致させ、ファイル欠落による起動不能を
  防ぎます。
- 「依存パッケージも再インストールする (pip install)」オプションを、`requirements.txt`
  が変更されたアップデートのときだけ表示するようにしました。
- 書き出し用レンダラを `antialias:false` + `preserveDrawingBuffer:false` に分岐
  (プレビューは従来どおり)。Windows で毎フレームの読み出しが MSAA 解決で直列化して
  遅くなる問題を解消しました。

### Fixed
- Windows でサーバ起動直後やアップデート時に、ffmpeg/ffprobe の UTF-8 出力を cp932 で
  デコードしようとして `UnicodeDecodeError` で落ちる問題 (全同期 subprocess を utf-8
  固定)。
- Windows で autoreload 起動時に動画書き出しが `FFMPEG_SPAWN_FAILED` になる問題
  (reload を自動無効化)。
- 背景動画が後半のカットで消える問題 (WebCodecs デコードの出力サーフェスプール枯渇)。
- 動画レイヤーのデコードが特定環境で停止/ハングする問題に対し、deadline 打ち切りと、
  失敗時はそのレイヤーをスキップして書き出しを完走させるフェイルセーフを追加。
- 書き出しのエンコーダ表示が、プリセット利用時に実際と異なるエンジン名 (videotoolbox)
  を出していた誤表示を修正。
- プレビューの smooth モードでキャラクターのドラッグが効かない問題を修正。

### 注記 (既知の制限)
- Windows のブラウザ内 H.264 エンコードはソフトウェア実装になる環境があり、その場合は
  書き出し速度に上限があります (macOS の HW エンコードほどは出ません)。サーバ GPU
  エンコード (NVENC) を活かす中間形式経路は今後検討します。

(本リリースは py_tachie/dev チャネルで先行配信していた一連の変更を、Mac/Windows
両方での検証を経て stable に取り込んだものです。)

## [0.1.3] - 2026-05-25

### Fixed
- 動画レイヤーを多数 (例: 10 本以上) 含むプロジェクトの長時間プレビュー再生で、Chrome のメモリ消費が常時増加していた問題を構造的に解消。シーン全体の動画レイヤーすべてに対して `<video preload=auto>` と clean PCM `<audio preload=auto>` を常駐させていた経路を、「現カット ± lookahead カット」の時間窓内のみに絞るよう変更しました。窓外の `<video>` / `<audio>` / VideoTextureProvider は dispose されます。
- Windows で `project.json` の保存と読み込みが極短時間に並列衝突し、`PermissionError: [Errno 13]` でサーバが例外を投げる (= プレビューが詰まる + ターミナルの Ctrl+C が効かなくなる) 不具合を修正。`write_project_file` を per-project の Lock で直列化し、`read_project_file` の retry に `PermissionError` を追加しました。再生中の playhead 連続保存で踏みやすかった経路です。
- `stopPreviewPlayback()` で pre-built SceneInstance キャッシュも一緒にクリアするように変更 (停止 → 編集 → 再生で古い prefetch が残らないように)。

### Added
- 開発者向け観測ログ `window.__spliteVLPerf = true`。再生中、1 秒間隔で `state.playbackVideoLayerEls` 等の Map サイズと dedup 後の実リソース数、prefetch キャッシュ数、VL group 数を Console に出力します。既存の `window.__spliteCutPerf = true` と組み合わせて切替コストを観測できます。

(本リリースの内容は v0.1.2 公開後に py_tachie/dev チャネルで先行配信していた変更を、Mac/Windows 両方での検証完了を受けて stable に取り込んだものです。)

## [0.1.2] - 2026-05-24

### Added
- ツールバー overflow (`>>`) メニュー: ウィンドウ幅が足りないとき、収まらないボタンを右端の「>>」ボタンを押すとプルダウンメニュー表示する動的振り分け機構を実装。タイムラインが押し下げられず常に 1 段に収まります。サブメニュー (例: 「アイテムを追加」) は overflow に入った場合、右側へ flyout します。

### Changed
- 上段 / 下段ツールバーを `flex-wrap: nowrap` 固定にしてボタンの縦書き崩れを回避。

(本リリースの内容は v0.1.1 公開後に py_tachie/dev チャネルで先行配信していた変更を、検証完了を受けて stable に取り込んだものです。)

## [0.1.1] - 2026-05-24

### Added
- 受信チャネル選択 (stable / dev)。全体設定 → アップデートに `受信チャネル` セレクタを追加。`stable` (= origin/main、安定版) と `dev` (= origin/dev、ナイトリービルド) を切替可能。検証中コードを含むため既定は `stable`。
- `/api/update/{check,apply}` に `channel` パラメータ対応。target branch がローカルに無い場合は origin から自動 checkout して切替する。

### Docs
- user_guide のアップデート章に dev チャネル (ナイトリービルド) 注意書きを追記。

## [0.1.0] - 2026-05-24

初回公開リリース (Private)。初回 commit (2026-05-22) からタグ付けまでに NFC 正規化補強・アプリ内アップデータ・ビジュアライザーのレイヤー順再構成 (`below_bg` 新設)・カット切替時の音声途切れ解消 (audio.play() と buildScene の並列化)・scene 事前 build memory cache・プレビュー表示品質オプション (sharp/smooth)・キャッシュ retention の時間単位化、などを取り込んでいます。

### 主な機能

- **PSD レイヤー付きキャラクター** の取り込みと立ち絵合成
  - 表情プリセット (目・口・髪) の組み合わせ管理
  - キャラクター単位の `removeWhite` / `antialiasBlackLine` 設定
  - 口パク (`speakerCharacterId` 単体)・目パチ (表示中キャラ全員) の自動同期
- **シナリオ編集 UI** (vanilla HTML/CSS/JS、フレームワーク非依存)
  - v3 シナリオフォーマット (`state.characters` 配列形式) / 旧形式からの自動変換
  - カット・テロップ・効果音・BGM トラック・動画レイヤー・ビジュアライザの統合タイムライン
  - スナップ (再生ヘッド/カット境界/BPM/他端/グリッド) + クロスタイプ選択 + リンク追従
  - 自動バックアップ (周期/切替時/復元前)、複数世代の保持と一覧復元
- **WebGL ベースのプレビュー** (Three.js)
  - リアルタイム再生・PNG 書き出し・動画書き出し・サムネ生成を統一経路で実行
  - シーンバンドル機構 (`sceneOverride` で disk 読込より live state を優先)
- **1920×1080 MP4 書き出し**
  - HW エンコーダ優先 (h264_videotoolbox / nvenc) + ソフトウェアフォールバック
  - 録画素材の PTS gap 検査と再エンコード正規化
  - クリーン PCM 経由の音声同期 (録画素材の AAC PTS gap 対策)
- **TTS 連携**
  - VOICEVOX / Voicepeak (style / emotion をキャラ別に保存)
  - 音声プロジェクト経由の素材管理と未使用フィルタ
- **テキストエフェクトシステム**
  - kind = caption / mv_text / composition の 3 区分
  - エフェクトプリセット 8 種: `fade_slide` / `typewriter` / `neon_glow` / `rgb_shift` / `pop_per_char` / `glitch_scan` / `shake_beat` / `huge_handwritten` (撤去) / `poster_typography`
  - アニメーション in/out/body の独立スロット
  - サーバ側で禁則処理 (`LINE_START/END_FORBIDDEN`)
- **タイトル組版エディタ** (`/title-editor`)
  - プロジェクト横断ストア (`title_compositions/`)
  - ink-aware bbox によるピクセル単位の整列・スナップ
  - 位置プリセット 3×3 / 光彩 / ドロップシャドウ / オプティカルカーニング
- **ビジュアライザプラグイン** (13 種)
  - 波形・スペクトラム・パーティクル・LED グリッド・ジオメトリック背景等
- **多言語対応の準備**
  - 日本語 (LINE Seed JP / Shippori Mincho B1 / M PLUS Rounded / Zen Maru Gothic) を想定したフォント設計
  - ライト / ダークテーマ切替 + `prefers-color-scheme` 連動

### 非機能

- ライセンス: MIT
- 出力解像度: 1920×1080 固定
- 依存: Python 3.11+ / Pillow / FastAPI / ffmpeg (システムインストール)
- 同梱しないもの:
  - フォント (全体設定からユーザーがインストール)
  - 効果音 (将来的にコード生成版で再導入予定)
  - その他キャラクター素材 (権利上の理由)
- 同梱するもの:
  - キャラクター素材: `maki_py`, `moca_py` (作者: pyoru0309 / 改変自由)
  - 背景・前景・装飾画像・音声: `assets/audio`, `backgrounds`, `foregrounds`, `overlays` (作者: pyoru0309 / 改変自由)

[Unreleased]: https://github.com/pyoru0309/py_tachie/compare/v0.4.0...HEAD
[0.4.0]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.4.0
[0.3.0]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.3.0
[0.2.2]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.2.2
[0.2.1]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.2.1
[0.2.0]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.2.0
[0.1.3]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.1.3
[0.1.2]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.1.2
[0.1.1]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.1.1
[0.1.0]: https://github.com/pyoru0309/py_tachie/releases/tag/v0.1.0
